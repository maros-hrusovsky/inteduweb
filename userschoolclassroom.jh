entity School {
	name String
}

entity Classroom {
	name String
}

relationship ManyToMany {
	Classroom{users(firstname)} to User{classrooms(name)}
}

relationship OneToMany {
	School{classrooms(name)} to Classroom{school(name)}
}
relationship ManyToOne {
	User{school(name)} to School{users(firstname)}
}
